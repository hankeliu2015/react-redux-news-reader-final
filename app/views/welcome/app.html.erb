<h4><%= "Hello " + "#{current_user.username} !" if current_user %></h4>

<%= link_to "Login", new_user_session_path, class: "nav-item nav-link" %>

<%= link_to "Sign Up", new_user_registration_path, class: "nav-item nav-link" %>

<%= link_to 'Sign Out', destroy_user_session_path, method: :delete, class: "nav-item nav-link" if user_signed_in? %>

<div id="root"></div>

<%= javascript_pack_tag 'index' %>
